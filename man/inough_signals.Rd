% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inough_signals.R
\name{inough_signals}
\alias{inough_signals}
\title{Extract inattention signals}
\usage{
inough_signals(
  df,
  correct,
  response,
  id = "ID",
  learning_effect = TRUE,
  participant_effect = TRUE,
  trial_transform = "sqrt"
)
}
\arguments{
\item{df}{A data frame with rows ordered by trial within each participant.}

\item{correct}{Formula. LHS names the accuracy column (0/1 integer). RHS
names design predictors that explain correctness (e.g.,
\code{correct ~ Stim + Weight + Orient + Block}).}

\item{response}{Formula identifying the response column. Use a two-sided
formula where the RHS names the column (e.g., \code{response ~ answ}).
Must have exactly 2 distinct values; auto-encoded to 0/1 by sorted order.}

\item{id}{String naming the participant identifier column (default
\code{"ID"}).}

\item{learning_effect}{Logical. If \code{TRUE} (default), adds \code{n_trial}
as fixed effect and random slope per participant.}

\item{participant_effect}{Logical. If \code{TRUE} (default), adds random
intercept per participant.}

\item{trial_transform}{Transformation applied to trial index before rescaling
to [-1, 1]. One of \code{"sqrt"} (default), \code{"log"}, \code{"linear"},
or a function.}
}
\value{
An \code{inough_signals} object.
}
\description{
Fits a probit GLMM on accuracy and computes response bias indicators.
This is the first step in the inough pipeline: call \code{inough_signals},
then pass the result to \code{\link{inough_detect}}.
}
